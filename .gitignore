# Java build output (sdk/ is source; built jars live in <scenario>/sdk-dist/ and ARE committed)
target/
*.class
dependency-reduced-pom.xml

# Examples runtime output
sdk/examples/output/

# IDE
.idea/
*.iml
.vscode/
.classpath
.project
.settings/

# Deploy runtime: local env + bind-mounted data/logs (DB data, secrets — never commit)
*/deploy/.env
*/deploy/data/
*/deploy/logs/

# OS
.DS_Store
